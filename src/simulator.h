#ifndef SIMULATOR_H
#define SIMULATOR_H

#include <string>

/**
 * Carrega o arquivo .obj e simula a execução do programa.
 * Interage com o usuário através de comandos INPUT (teclado) e OUTPUT (monitor).
 */
void runSimulator(const std::string& filename);

#endif